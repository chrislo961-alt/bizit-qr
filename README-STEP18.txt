SoloBizKit Step 18 — Brand + Social Sharing

Upload ALL contents of this ZIP to the repository ROOT and overwrite matching files.

Included:
- Consistent social preview image
- Open Graph image metadata
- Twitter/X large-card metadata
- og:site_name metadata
- brand.css with shared SoloBizKit brand tokens
- Existing favicon + web manifest preserved
- Existing Analytics, consent, SEO, performance and user-flow work preserved

HTML pages patched: 37

Social preview:
https://solobizkit.it.com/assets/images/solobizkit-social-preview.png

After deploy:
1. Open the social-preview URL directly and confirm the image loads.
2. Share the homepage URL in a private message/chat that generates link previews.
3. Test an inner tool page such as /invoice-generator/.
4. Social networks may cache an old preview for a while; that does not necessarily mean the metadata is wrong.

Note:
The generated social image is intentionally a broad SoloBizKit brand card. Later we can create unique OG images for the highest-traffic tools once Search Console tells us which pages deserve them.
