const btn=document.getElementById('themeBtn');
if(btn){btn.addEventListener('click',()=>{document.body.classList.toggle('dark');alert('Theme toggle UI is ready. Add a full dark palette when you want it.');});}
