SoloBizKit Step 14 – Paycheck SEO Cluster

Upload everything to the repository ROOT.

Adds:
- /salary-paycheck-calculator/
- /hourly-paycheck-calculator/
- /biweekly-paycheck-calculator/
- /monthly-paycheck-calculator/
- paycheck-seo.css
- updated sitemap.xml

Why no state-by-state pages yet:
State payroll/tax calculations require jurisdiction-specific rules and frequent updates. These four pages target useful search intent without presenting generic state-tax estimates as exact.

The new pages also include the Step 13 Analytics consent files if analytics.js and analytics-consent.css are already present at the site root.
